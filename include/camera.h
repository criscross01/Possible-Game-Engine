#ifndef CAMERA_H
#define CAMERA_H
#include <glm/glm/glm.hpp>

class Camera
{
    public:
        Camera(glm::vec3(int,int,int),glm::vec3());
        virtual ~Camera();

    protected:

    private:
};

#endif // CAMERA_H
